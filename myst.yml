# See docs at: https://mystmd.org/guide/frontmatter
version: 1
project:
  id: jupyterbook-wolfram-demo
  # title:
  # description:
  # keywords: []
  # authors: []
  # github:
  # To autogenerate a Table of Contents, run "myst init --write-toc"
  plugins:
    - type: executable
      path: plugins/wolfram.py
site:
  template: book-theme
